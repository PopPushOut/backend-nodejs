extends layout

block content
  .inner
    form.form(action='/transactions', method='POST')
      h2= title
      label(for="receiver") Receiver
      input(type='text', name='receiver' required)
      label(for="transfer_amount") Transfer Amount
      input(type='text', name='transfer_amount' required)
      input.button(type='submit' value="Send →")
  